using System;

namespace FeedTheCat
{
    /// <summary>
    /// Enum representing game difficulty levels.
    /// Used to determine reward rates and quantities.
    /// </summary>
    public enum Difficulty
    {
        Easy = 0,
        Medium = 1,
        Hard = 2
    }
}
