using System;

namespace FeedTheCat.Items
{
    /// <summary>
    /// Enum for item types: Active items can be used, passive items cannot.
    /// </summary>
    public enum ItemType
    {
        Active = 0,
        Passive = 1
    }
}
