using System;

namespace FeedTheCat.Items
{
    /// <summary>
    /// Enum for NPC target types that items can affect.
    /// </summary>
    public enum TargetType
    {
        NPC = 0,
        Player = 1,
        Mixed = 2
    }
}
